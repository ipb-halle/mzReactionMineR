% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/louvain_clustering_features.R
\name{louvain_clustering_features}
\alias{louvain_clustering_features}
\title{louvain_clustering_features}
\usage{
louvain_clustering_features(
  object,
  assay,
  R_trsh = 0.8,
  resolution = 1,
  min_degree = 20,
  min_cluster_size = 50,
  metric = "pearson",
  type = "pos",
  filter_type = "intensity",
  n_top = nrow(object),
  return_removed = TRUE,
  calc_PCA = FALSE,
  min_PC = 5,
  PC_var = 0.8,
  scale = TRUE,
  id_col = "id"
)
}
\arguments{
\item{object}{a SummarizedExperiment object}

\item{assay}{Character. The name of the assay to be used for clustering.}

\item{R_trsh}{Numeric. Correlation threshold for an edge to be drawn.}

\item{resolution}{Numeric. Resolution parameter for the Louvain algorithm.
Default is 1.}

\item{min_degree}{Integer. minimum number of edges for a feature to be kept.}

\item{min_cluster_size}{Integer. Minimum cluster size to be kept after clustering.}

\item{metric}{Either "pearson", "kendall", or "spearman". The correlation
metric to use for calculating the correlation matrix. Default is "pearson".}

\item{type}{Either "pos", "neg", or "both". What kinds of correlation will be
used for the graph: only positive, only negative or both.}

\item{filter_type}{Character. The method to select features for clustering.
Must be either "intsnity" or "variance". Default is "area".}

\item{n_top}{Integer. The number of features to select based on
filter_type.}

\item{return_removed}{Logical. Whether to return features removed during the clustering process.
removed features will be added as cluster -1.}

\item{calc_PCA}{Logical. Wether to perform PCA before clustering.
Default is TRUE.}

\item{min_PC}{Integer. The minimum number of PCs to retain. Default is 5.}

\item{PC_var}{Numeric. The minimum cumulative variance that the retained PCs should explain.}

\item{scale}{Logical. Whether to scale the data before calculating the
distance matrix. Default is TRUE.}

\item{id_col}{Character. The name of the column in rowData(object) that
contains the feature ids. Default is "id".}
}
\value{
a data.frame with feature ids, cluster labels, and within-cluster connectivity.
}
\description{
A function that performs clustering (community detection) on features in a
SummarizedExperiment object based on correlation across samples.
}
