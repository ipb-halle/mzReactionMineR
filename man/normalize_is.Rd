% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/normalize_is.R
\name{normalize_is}
\alias{normalize_is}
\title{normalizeIS}
\usage{
normalize_is(
  object,
  assay = NULL,
  rt = NULL,
  mz = NULL,
  id = "none",
  type = "median",
  mz_tolerance = c(0.005, 10),
  rt_tolerance = 0.1,
  new_assay_name = "is_normalized",
  remove = TRUE,
  id_col = "id",
  rt_col = "rt",
  mz_col = "mz"
)
}
\arguments{
\item{object}{A SummarizedExperiment object}

\item{assay}{Character. The name of the assay to be normalized.}

\item{rt}{Numeric. The theoretical retention time of the internal standard.}

\item{mz}{Numeric. The theoretical m/z of the internal standard.}

\item{id}{Character. The id of the internal standard. If "none", the function
will find the closest feature to the provided rt and mz values.
Default is "none".}

\item{type}{Either "mean" oder "median". How to calculate the reference IS
value for normalization. Default is "median".}

\item{mz_tolerance}{Numeric vector of length 2. Absolute and relative (ppm)
m/z tolerance for finding the internal standard.}

\item{rt_tolerance}{Numeric. Absolute retention time tolerance for finding
the internal standard.}

\item{new_assay_name}{Character. The name of the new assay that will contain
the normalized intensities. Default is "is_normalized".}

\item{remove}{Logical. Whether to remove the internal standard from the
object after normalization. Default is TRUE.}

\item{id_col}{Character. The name of the column in rowData that contains the feature ids.}

\item{rt_col}{Character. The name of the column in rowData that contains the retention times.}

\item{mz_col}{Character. The name of the column in rowData that contains the m/z values.}
}
\value{
A SummarizedExperiment object with a new assay containing the
normalized intensities.
}
\description{
A function to normalize the intensities of an assay in a SummarizedExperiment
object based on the intensities of an internal standard. The internal
standard can be specified by its id, or by its rt and mz values. The
function will find the closest feature to the provided rt and mz values, and
use its intensities for normalization.
}
\details{
Normalization is defined as:

\deqn{\hat{Y_ij} = Y_ij\frac{\overline{S}{S_j}}}

Where \eqn{\hat{Y_ij}} is the normalized value of peak i in sample j,
\eqn{Y_ij} is the original value  of peak i in sample j, \eqn{S_j} is the
internal standard intensity in sample j and \eqn{\overline{S}} is the mean
or median of the internal standard across samples.
}
